# 11. 양자 실험 설계실

- 프로젝트: `quantum-experiment-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 11
- 대상: 고등학교 심화 물리·수학 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문·차별성

‘한 번에는 점 하나인데, 반복하면 왜 간섭무늬가 나타날까?’ 개별 검출 사건과 누적 확률분포의 차이를 탐구한다.
기존 광학 앱의 광선 경로와 달리 확률진폭의 합성과 검출 표본을 다룬다.
11순위 이유는 시각적 효과가 강한 만큼 입자 경로·관측의 의미를 잘못 설명할 위험을 검토해야 하기 때문이다.

## 2. 학습 목표·오개념

- 진폭의 합과 확률의 합이 다를 수 있음을 비교한다.
- 개별 검출 위치를 예측하는 것과 누적 분포를 예측하는 것을 구분한다.
- ‘사람이 쳐다봐서 물리 현상이 바뀐다’, ‘점이 움직인 궤적을 알고 있다’를 방지한다.
- 경로 정보 조건은 환경과의 상호작용을 단순화한 모형이며 의식의 효과로 설명하지 않는다.

## 3. 흐름

1. 3분: 두 슬릿의 개별 분포를 보고 둘을 열었을 때를 예상한다.
2. 5분: 검출 1회·100회·1000회를 누적해 비교한다.
3. 5분: 상대 위상을 바꾸어 무늬 위치 변화를 관찰한다.
4. 7분: 간섭항이 있는 경우와 없는 모형을 같은 시행 수로 비교한다.
5. 5분: 단일 점과 전체 분포의 차이를 설명한다.
- 전이: 한 슬릿을 닫은 경우와 경로 정보가 있는 두 슬릿을 구분한다.

## 4. 화면·상태

| 화면 | 내용 | 행동 |
|---|---|---|
| 예측 | 슬릿 조건 | 예상 분포 선택 |
| 장치 | 슬릿·검출면 | 한 번 검출·반복 |
| 누적 | 점·히스토그램·이론 분포 | 시행 수 변경 |
| 조건 비교 | 위상·간섭 조건 | 새 실행 |
| 기록 | 관찰·한계 | 저장 |

ready → sampling → paused/completed → review. 조건 변경 시 이전 점을 새 분포에 섞지 않고 실행을 분리한다.
기존 실행과 새 실행의 조건·seed·N을 항상 표시한다. 빠른 실행은 점 이동 애니메이션 없이 누적한다.

## 5. 확률 모형

- P0는 1차원 검출면의 교육용 두 경로 진폭 모형이다. 완전한 슈뢰딩거 방정식 수치해석은 아니다.
- x∈[-10,10] 모형 길이, 512개 bin. g(x)=exp(-x²/(4σ²)), σ=3.
- ψ1=g(x)exp(iκx/2), ψ2=g(x)exp(-iκx/2+iφ).
- κ∈[0.5,4] rad/모형길이, φ∈[-π,π]. 실제 슬릿 폭·파장·거리로 직접 표기하지 않는다.
- 간섭 유지: rawP=|ψ1+ψ2|². 간섭 소실 모형: rawP=|ψ1|²+|ψ2|².
- 한 슬릿: rawP=|ψ1|². 각 조건은 현재 검출 영역의 합으로 정규화해 P_i=rawP_i/ΣrawP_i.
- 이는 ‘검출된 사건의 위치 분포’에 조건부인 모델이다. 광량·통과율·총 검출률 차이는 P0에서 비교하지 않는다.
- P1의 부분 간섭 계수 γ∈[0,1]는 rawP=|ψ1|²+|ψ2|²+2γ Re(ψ1*conj(ψ2)). P0에는 0/1만 제공한다.
- 누적분포에서 seed 난수로 bin을 추출한다. bin 안 위치는 추가 균등 난수를 쓰거나 중심으로 고정하고 방식을 기록한다.
- N∈{1,100,1000,10000}. 정규화 합이 0 또는 비유한 값이면 샘플링을 중단한다.
- 점의 화면 출현은 검출 사건이며 슬릿에서 검출면까지의 실제 궤적을 그리지 않는다.

## 6. 데이터·평가

- Parameters: sigma, kappa, phase, mode, detectorRange, bins.
- Distribution: rawWeights, normalizedProbabilities, cumulativeProbabilities.
- SampleRun: seed, parameters, counts, N, modelVersion.
- 비교 지표는 bin 비율과 이론 분포 차이이며 시행 수가 늘면 모든 bin 오차가 매번 줄어든다고 주장하지 않는다.
- 자동 평가: 조건 구별·확률 합 계산·동일 N 비교. 자유 설명은 사람 관찰과 물리적 상호작용 구별을 본다.

## 7. 시각화

Canvas 누적 점과 그래프가 핵심이다. Three.js 장치는 선택적 맥락이며 물리 경로를 보여주지 않는다.
장치의 크기는 실제 축척이 아니고 P0 변수와 실제 길이의 대응을 암시하지 않는다.
Canvas 실패 시 bin별 빈도 표와 SVG 확률분포를 제공한다.

## 8. 생성 이미지

- P0 실험실 도입 3장, 연구 카드 3장=6장. 확장 12장.
- 예시: ‘밝은 가상 양자 실험실 도입 삽화, 단순한 광원과 검출 장치, 입자의 이동 궤적·수식·간섭무늬는 그리지 않음’.
- 검출 점·간섭 그래프·진폭 도식은 엔진으로 생성한다.
- 검수: 입자를 작은 공의 확정 궤적으로 그린 그림, 관찰자 눈이 무늬를 바꾸는 연출을 제외한다.

## 9. 범위·단계

- P0: 한 슬릿/간섭 유지/간섭 소실, 위상, 표본 수, 동일 조건 재현, 비교.
- P1: 부분 간섭, 실제 원거리 슬릿 근사식, 유한 폭 효과. 단위와 가정 검토 후 추가한다.
- 제외: 양자 해석의 정답 판정, 실제 장비 제어, 양자컴퓨터 회로, 임의 퍼텐셜 해석.
- M1: 복소수 진폭·확률 → M2: 정규화·표본 → M3: 비교 UI → M4: 장치 맥락 → M5: 오개념 검토.
- 모듈: amplitudes.ts, distribution.ts, sampler.ts, experiments.ts, DetectorView.tsx, ProbabilityPlot.tsx. 각각 500줄 미만.

## 10. 검증·완료

- 모든 모드에서 확률≥0, 합 1(오차 1e-12 이하 목표), CDF 마지막 값 1.
- φ=0의 간섭 분포는 x=0에서 중심 최대, φ=π는 중심 최소.
- 간섭 소실 모드에서는 φ를 바꿔도 분포 불변. 한 슬릿도 φ와 무관하다.
- 같은 seed의 100회 결과는 1000회 실행 앞 100회와 일치하도록 난수 소비 규칙을 고정한다.
- 빈도 통계는 다중 seed 기준으로 검증하고 작은 N을 이론 곡선과 정확히 맞추지 않는다.
- 키보드 실행·320px 그래프·reduced-motion·표 대체에서 학습 가능해야 한다.
- gi-pulse는 누적/비교 버튼, 업데이트 내역 제공, VoiceOver 제외.
- 완료: 실제 확률 샘플러를 사용하고 진폭·분포·개별 사건을 구분하며 경로를 꾸며내지 않는다.
