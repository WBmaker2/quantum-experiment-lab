## 2026-09-23

- 양자 실험 설계실 P0 첫 화면을 만들었다. 한 번에 점 하나씩 검출하고, 100·1000·10000번 쌓으면 이론 분포와 비교할 수 있다.
- 예측 → 장치·누적 → 조건 비교(A·B) → 기록 저장 순서로 25분 활동을 따라간다. 궤적 선은 그리지 않고 점만 찍는다.
- 같은 seed면 같은 결과가 재현된다. φ=0은 가운데 최대, φ=π는 가운데 최소, 간섭 소실 모드는 φ와 무관함을 확인했다.
- 휴대폰(320·390px)·태블릿·데스크톱에서 가로 넘침 없이 보인다. 키보드만으로 예측·실행·기록이 된다.
- Canvas가 안 되면 SVG·표로 대신 보여준다. 다음 행동 버튼 하나에만 빛나는 테두리를 쓰고, 움직임 줄이기 설정에서는 정적 테두리로 바뀐다.
- 배포 직전 다듬기를 했다. 실행 상태(대기·계산 중·일시정지·취소·완료)를 글로 보여주는 뱃지를 달았고, 짧은 실행을 취소했을 때 ‘계산 중’에 멈추던 문제를 고쳤다.
- 엔진 자동 검사 9개를 넣었다 (`npm test`). 확률 합·위상 조건·seed 재현·입력 가드를 매번 확인한다.
- 실행·검사·배포 방법을 README에, 검증 결과를 VERIFICATION에, 배포 전 체크리스트를 DEPLOY에 적었다. 화면 캡처는 docs/qa에 모았다.
- 교과 검토를 했다. "간섭 소실 = 두 봉우리" 설명이 모형과 어긋나서 "무늬 없는 하나의 봉우리"로 바로잡고, 한 슬릿과 모양이 같은 이유(통과율 차이는 P1)를 밝혔다. 점의 세로 위치는 의미 없음을 적고, 확률·오차는 소수로 보여준다. 검토서는 CURRICULUM-REVIEW에 있다.
- 깃허브에 올리고 Pages로 공개했다. https://wbmaker2.github.io/quantum-experiment-lab/ 에서 바로 쓸 수 있다. 공개 주소에서도 실행·화면·파일이 깨지지 않음을 확인했다.
